Place official `sing-box` Android binaries here before running VPN:

- `android/arm64-v8a/sing-box`
- `android/armeabi-v7a/sing-box`
- `android/x86_64/sing-box`

Required:
- Filename must be exactly `sing-box`.
- Keep executable-format binaries for each ABI you target.
